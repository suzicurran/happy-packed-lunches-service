{
  "name": "happy-packed-lunches-service",
  "version": "1.0.0",
  "description": "a lunch tracking app",
  "engines": {
    "node": "12.2.0"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "debug": "node --inspect server.js",
    "dev": "nodemon --exec \"heroku local\" --signal SIGTERM",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "sucurran",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-session": "^1.16.1",
    "mongoose": "^5.5.11",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "nodemon": "^1.19.1"
  }
}
